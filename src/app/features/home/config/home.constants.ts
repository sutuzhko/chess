/** Числовые параметры главного экрана. Все «магические числа» — здесь. */
export const HOME_CONSTANTS = {
  /** Сколько последних завершённых партий показывать в карточке «Недавние партии». */
  recentLimit: 6,
  /** Окно для расчёта дельт ▲+N в плитках статистики (в днях). */
  deltaWindowDays: 7,
  /** Стартовый рейтинг тактики при первом запуске. */
  defaultRating: 1200,
  /** На сколько меняется рейтинг за одно успешное решение задачи. */
  ratingDeltaPerSolve: 8,
  /** Через сколько секунд после локальной полуночи разоружать ежедневный таймер. */
  midnightBufferSeconds: 5,
} as const;
