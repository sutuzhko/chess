/**
 * Глубина анализа для «Пусть решит ИИ». Существенно глубже, чем у игрового AI
 * (там макс. 8): движок должен сам найти лучший ход в учебных задачах.
 * Слишком высокая глубина рискует выйти за разумное время — поэтому есть
 * и time-cap ниже.
 */
export const PUZZLE_SOLVE_MAX_DEPTH = 32;

/**
 * Жёсткий потолок по времени для «Пусть решит ИИ» в миллисекундах.
 * Iterative deepening внутри движка остановится на лучшей доступной глубине,
 * когда упрётся в этот лимит.
 */
export const PUZZLE_SOLVE_MAX_TIME_MS = 20_000;

/** Уровень AI-оппонента для скриптовых матовых задач (макс). */
export const PUZZLE_AI_LEVEL_MAX = 20;

/**
 * Имя маршрута и параметр режима для перехода «Продолжить игру с этой позиции».
 * Не дублируем имена руками в шаблонах.
 */
export const PUZZLE_CONTINUE_ROUTE = {
  name: 'lobby',
  mode: 'standard',
} as const;

/** Брейкпоинт схлопывания layout решателя в одну колонку. */
export const PUZZLE_SOLVER_SINGLE_COL_BREAKPOINT = '1024px';

/** Ширина правой панели задач. */
export const PUZZLE_SOLVER_SIDE_PANEL_WIDTH = '380px';
